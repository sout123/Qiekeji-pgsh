# 胖乖生活手机号验证码登录协议

本文档根据 `胖乖生活.har` 中观察到的请求整理，仅用于本人账号或已获授权的测试环境。

## 一、基础信息

- 服务地址：`https://userapi.qiekj.com`
- 请求格式：`application/x-www-form-urlencoded;charset=UTF-8`
- 登录方式：手机号 + 短信验证码
- 当前 HAR 未发现密码登录接口

登录接口还会校验移动端公共请求头，包括 `Version`、`channel`、`phoneBrand`、`deviceId`、`timestamp`、`sign` 和 `QEUser` 风格的 `User-Agent`。其中 `timestamp` 每次请求变化，`sign` 使用固定密钥和请求路径计算 SHA-256。

### 公共请求头

```http
Authorization:
Version: 1.139.0
channel: android_app
phoneBrand: OnePlus
deviceId: {设备ID}
timestamp: {当前毫秒时间戳}
sign: {动态签名}
User-Agent: QEUser/1.139.0 (com.qiekj.user; build:234; Android 16; userChannel:android_app; version:1.139.0) OkHttp/4.12.0
Content-Type: application/x-www-form-urlencoded;charset=UTF-8
Accept-Encoding: gzip
```

服务端可能通过 `Set-Cookie: acw_tc=...; Max-Age=1800` 下发风控会话 Cookie。客户端应使用同一个 Session 自动保存并回传 Cookie，不要手工写死 HAR 中的 Cookie。

### sign 生成算法

签名前文必须严格按以下顺序拼接，不能排序、不能 URL 编码、不能增加空格：

```text
appSecret={应用密钥}&channel=android_app&timestamp={毫秒时间戳}&token={Token}&version={版本}&{请求路径}
```

当前版本参数：

```text
应用密钥 = nFU9pbG8YQoAe1kFh+E7eyrdlSLglwEJeA0wwHB1j5o=
版本 = 1.139.0
```

计算方式：

```text
sign = SHA256_UTF8(签名前文).hexdigest().lower()
```

登录前示例路径为 `/common/isNeedCaptcha`、`/common/sms/sendCode` 或 `/user/reg`，登录前 `token` 为空字符串。登录后的请求使用当前 Token。`timestamp` 必须同时放入签名前文和请求头，且两处值完全一致。

## 二、登录流程

### 1. 检查图形验证码

```http
POST /common/isNeedCaptcha HTTP/1.1
Host: userapi.qiekj.com
Content-Type: application/x-www-form-urlencoded;charset=UTF-8

phone={手机号}
```

成功响应示例：

```json
{
  "code": 0,
  "msg": "成功",
  "data": false
}
```

当 `data` 为 `true` 时，服务端要求额外的图形验证码流程；HAR 中没有捕获到该流程的完整接口，因此客户端不应跳过验证。

### 2. 发送短信验证码

```http
POST /common/sms/sendCode HTTP/1.1
Host: userapi.qiekj.com
Content-Type: application/x-www-form-urlencoded;charset=UTF-8

phone={手机号}&template=reg
```

成功响应：

```json
{
  "code": 0,
  "msg": "成功",
  "data": null
}
```

### 3. 提交验证码登录

```http
POST /user/reg HTTP/1.1
Host: userapi.qiekj.com
Content-Type: application/x-www-form-urlencoded;charset=UTF-8

channel=android_app&phone={手机号}&verify={短信验证码}
```

成功响应：

```json
{
  "code": 0,
  "msg": "成功",
  "data": {
    "token": "{登录Token}",
    "newRegister": false,
    "forcedBind": false
  }
}
```

`/user/reg` 是注册/登录合一接口：

- `newRegister=false`：已有账号登录；
- `newRegister=true`：新用户注册后登录；
- `forcedBind=true`：需要继续绑定或补充信息。

响应中的 `t` 是服务端响应时间戳，不能当作登录 Token 使用。

## 三、后续接口鉴权

登录成功后，HAR 中的业务请求通常会把 Token 放在表单参数中：

```text
token={登录Token}
```

HAR 中后续请求存在两种表单编码：原生接口常见 URL 编码表单，WebView 页面常见 `multipart/form-data`。两者都需要携带 Token，不能把 Token 放进 URL 日志或公开链接。

部分请求同时携带 `Authorization` 请求头。实际调用时应以目标客户端当前版本的请求格式为准：

```http
Authorization: {登录Token}
```

示例：

```http
POST /user/info HTTP/1.1
Host: userapi.qiekj.com
Content-Type: application/x-www-form-urlencoded;charset=UTF-8
Authorization: {登录Token}

token={登录Token}
```

## 四、Python 使用

安装依赖：

```bash
python -m pip install requests
```

运行：

```bash
python 登录协议.py
```

也可以直接传入手机号：

```bash
python 登录协议.py --手机号 你的手机号
```

脚本默认只显示脱敏后的 Token。确需在本地调试时，可使用 `--显示令牌`，但不要把终端输出、Token 或验证码提交到 Git、日志平台或公共聊天中。

## 五、错误处理建议

1. 先检查 HTTP 状态码；
2. 再检查业务字段 `code` 是否为 `0`；
3. `data=true` 时暂停流程，完成图形验证码；
4. 对短信发送和验证码提交增加频率限制，避免触发风控；
5. Token 只保存在内存或系统安全存储中，不要硬编码。

如果服务端返回 `405 Not Allowed`，优先检查是否缺少上述移动端公共请求头；如果补齐后仍失败，通常需要从本人客户端或授权的 APK 中还原 `sign` 生成算法，不能使用 HAR 中过期的旧签名。

## 六、orderRiskSign

定位风控接口还会额外生成 `orderRiskSign`，它与普通 `sign` 是两套签名。其拼接格式为：

```text
appSecret={LOCATION_SECRET}&categoryCode={categoryCode}&channel=android_app&imei={imei}&lat={lat}&lng={lng}&timestamp={毫秒时间戳}&token={Token}&version={版本}&{请求路径}
```

其中 `LOCATION_SECRET` 与普通 `appSecret` 当前相同，结果使用同样的 SHA-256 算法，并同时发送：

```http
orderRiskSign: {签名结果}
orderRiskTimestamp: {同一个毫秒时间戳}
```

它只适用于定位/风控相关接口，不应添加到普通登录请求中。

## 七、仍需注意的运行条件

1. `deviceId` 实际来自客户端的 OAID，可能与设备或安装实例绑定；
2. `timestamp` 需要使用客户端与服务端允许范围内的当前时间；
3. `Set-Cookie` 应由同一个 Session 自动维护；
4. 如果 `isNeedCaptcha` 返回 `true`，仍需要完成图形验证码流程；
5. 应用升级后应重新确认版本号、User-Agent 和签名密钥是否变化。
