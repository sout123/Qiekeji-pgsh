# 胖乖生活协议脚本使用说明

本项目包含一个基于当前新版接口整理的 Python 脚本，用于在本人账号和本人有权使用的饮水设备上完成登录、余额查询、扫码识别、解锁、使用状态判断和订单查询。

## 文件说明

- `登录协议.py`：Python 命令行脚本。
- `登录协议文档.md`：接口、参数、签名和流程说明。
- `登录状态.json`：脚本运行后生成的本地登录态文件，包含敏感凭据，不应分享或提交到 Git。

## 环境要求

- Python 3.10 或更高版本。
- 可以访问目标服务的网络环境。
- 本人账号、有效短信验证码和本人设备 ID。
- `requests` 依赖。

安装依赖：

```powershell
python -m pip install requests
```

## 基本运行

在项目目录执行：

```powershell
python 登录协议.py
```

脚本会依次询问：

1. 手机号；
2. 客户端设备 ID；
3. 短信验证码；
4. 可选的扫码结果 URL；
5. 是否使用积分抵扣。

登录成功后，脚本会自动查询并显示余额、积分和积分可抵扣金额。

## 使用已保存登录态

首次登录成功后，脚本会在同目录生成 `登录状态.json`。再次运行时，如果登录态仍然有效，会跳过短信登录并直接查询余额。

指定扫码结果并使用积分：

```powershell
python 登录协议.py --扫描链接 "https://h5.qiekj.com/skip?SN=你的设备SN" --使用积分
```

指定扫码结果但不使用积分：

```powershell
python 登录协议.py --扫描链接 "https://h5.qiekj.com/skip?SN=你的设备SN" --不使用积分
```

如果不指定 `--使用积分` 或 `--不使用积分`，脚本会在运行时询问。

## 重新登录和清除登录态

忽略已有登录态并重新登录：

```powershell
python 登录协议.py --重新登录
```

删除本地登录态：

```powershell
python 登录协议.py --清除登录态
```

也可以指定状态文件路径：

```powershell
python 登录协议.py --状态文件 "D:\临时文件\胖乖生活登录状态.json"
```

## 解锁流程

脚本的完整主流程如下：

```text
读取或完成登录
  → 查询余额和积分
  → 解析二维码中的 SN
  → 获取商品 ID
  → 获取商品详情和 SKU
  → 检查积分资格和风控
  → 设置支付渠道
  → 执行位置风控检查
  → 确认真实解锁
  → 解锁饮水机
  → 轮询设备使用状态
  → 实际使用后创建并同步订单
  → 查询最终订单详情
```

解锁是有真实副作用的操作。脚本要求输入大写 `YES` 才会发送解锁请求：

```text
确认执行真实解锁？请输入 YES：
```

输入其他内容会取消本次解锁。

## 状态判断

`/goods/water/sync` 返回的 `amount` 是设备使用量，不是人民币金额：

- `amount == 0`：已解锁，但尚未实际使用；
- `amount > 0`：已经实际使用；
- `amount == null` 或 `workStatus == 2`：仍在工作或结果尚未完成。

只有确认 `amount > 0` 后，脚本才会继续执行消费订单创建、同步和账单查询。

订单账单中的金额字段：

- `markPrice`：订单标价，也就是原价；
- `payPrice`：实际支付金额；
- `tokenCoinDiscount`：余额或积分抵扣金额；
- `waterUseValue`：订单记录的设备使用值，不是人民币金额。

## 定位风控参数

如果目标设备或服务端要求定位风控，可以手动提供定位参数：

```powershell
python 登录协议.py --扫描链接 "https://h5.qiekj.com/skip?SN=你的设备SN" --纬度 你的纬度 --经度 你的经度
```

定位参数应使用当前真实位置，并且只能用于本人有权操作的设备。不要在公开日志、截图或聊天记录中泄露精确定位。

## 常见问题

### HTTP 405 或请求被拒绝

检查以下内容：

- 设备 ID 是否来自当前客户端实例；
- 客户端版本、请求头和 User-Agent 是否匹配；
- 本机时间是否准确；
- 登录态和 Cookie 是否仍然有效；
- 当前网络是否被服务端风控拦截。

如果服务端要求图形验证码，脚本会停止流程。当前脚本没有实现图形验证码处理，应先在官方客户端完成验证。

### 登录态失效

执行：

```powershell
python 登录协议.py --重新登录
```

如果需要彻底删除本地凭据，执行：

```powershell
python 登录协议.py --清除登录态
```

### 订单仍在同步

订单同步是异步过程。脚本会有限次数轮询，超过次数后停止等待，不代表一定没有订单。可以稍后使用有效登录态再次查询，但不要重复执行解锁请求。

## 安全注意事项

- 不要把 `登录状态.json`、Token、Cookie、短信验证码或完整请求头上传到网盘、聊天群、日志平台或 Git 仓库。
- 不要在源代码中写入真实手机号、设备 ID、设备 SN 或定位坐标。
- 应用密钥、设备指纹和风控参数属于敏感信息，应仅保存在受控的本地环境。
- 共享日志前，应检查请求头、表单参数、响应内容和异常堆栈中的敏感字段。
- 项目仅实现当前材料确认的新版流程，接口、签名规则和风控策略变化后可能失效。

## 免责声明

本项目仅供本人账号、本人设备和已获得明确授权的测试环境进行技术研究、接口调试和自动化管理使用。

使用者必须自行确认：

1. 对所使用的账号、手机号、设备和网络环境拥有合法权限；
2. 已了解解锁请求可能启动真实饮水设备并产生订单或费用；
3. 不使用本项目绕过图形验证码、账号风控、设备权限、支付限制或其他安全措施；
4. 不对他人账号、设备、订单、Token、Cookie 或个人信息进行未授权访问；
5. 遵守服务提供方的用户协议、隐私政策及所在地适用的法律法规。

本项目不保证接口长期可用、请求一定成功、订单一定能够同步完成，也不对因使用本项目造成的设备损坏、费用、账号限制、数据泄露、隐私问题或其他直接、间接损失承担责任。任何真实解锁、积分抵扣、订单创建和费用产生行为，均由使用者在确认操作前自行判断并承担后果。

如果无法确认授权范围或操作可能影响他人设备、账号或费用，请不要运行解锁流程。
